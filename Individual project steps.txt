Individual project stepsData Preprocessing1.Transpose the dataset2. Drop unnecessary columns4. Create test set5. Run the diagnostics to determine the best approach to handle 80% missing data for MNAR case6. Apply the approach7. Create latent variable8. Feature engineering8. Perform encoding for any remaining/added categorical variables country/industry/year..? to generate fixed effects9**. Do more EDA to pick best algorithm?**    **-Candidate models are: Linear SVM, Linear Regression, CART, Random Forest, iKNN**    \*\*-Final pick: RF + SHAP\*\*10. If I choose tree regression, run either grid search/Bayesian optimizer/etc. to tune hyperparameters11. Apply the model on the train test and set iterations on the validation set12. Run model evaluation on validation set ONCEToday:1.Tune hyperparameters2. Fit the model on the train, fine tune a little bit using validation/test scores3. Convergence method?4. SHAP Analysis + Interpretation##############################################################################################################Target variable analysis:Exporter: general 'umbrella term'--> average of mean value per surviving entrant, entrant and incumbentcould use the decomposed export value terms are predictors while keeping exporter as outcomedoing some net value of export growth from existing exporters - cumulative export growth from exiters?*try uv.lock when testing with different models C